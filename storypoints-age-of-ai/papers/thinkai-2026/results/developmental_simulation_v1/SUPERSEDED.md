# Superseded

This first reporting render is not for manuscript use because visual inspection
found overlapping footer and axis text. Use
`../developmental_simulation_v2/`. The source simulation inputs are unchanged;
the corrected package has its own implementation hash and output manifest.

