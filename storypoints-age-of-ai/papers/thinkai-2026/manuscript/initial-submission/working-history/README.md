# Superseded Working Builds

These files are retained only to document the local render-and-correction loop.
They are not submission candidates and must not be distributed. The canonical
working anonymous package is the v0.2 `FINAL` DOCX/PDF pair one directory up.

- v0.1 exposed a continued reference-numbering defect and a split results row.
- the unsuffixed v0.2 DOCX is the pre-metadata-scrub builder output.
